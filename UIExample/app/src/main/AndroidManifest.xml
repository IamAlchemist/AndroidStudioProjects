<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tiny.wizard.uiexample" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PopMenuActivity"
            android:label="@string/popup_menu"/>

        <activity android:name=".PopWindowActivity"
            android:label="@string/popup_window"/>

        <activity android:name=".SectionListActivity"
            android:label="@string/section_list"/>

        <activity android:name=".CustomSectionActivity"
            android:label="@string/custom_section_list"/>

        <activity android:name=".CircleBarActivity"
            android:label="@string/circle_progress_bar"/>

        <activity android:name=".NavTabActivity"
            android:label="@string/navigation_tab_bar"/>

        <activity android:name=".WheelIndicatorActivity"
            android:label="@string/wheel_indicator"/>

    </application>

</manifest>
